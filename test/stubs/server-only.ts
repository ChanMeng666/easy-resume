// Test stub for the `server-only` package, which throws when imported outside a
// Next.js server bundle. In unit tests we import server modules directly, so we
// alias `server-only` to this no-op (see vitest.config.ts).
export {};
